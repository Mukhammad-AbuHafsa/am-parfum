import { qs, qsa } from '../core/dom.js';
export function initCustomerGate() { const gate=qs('#customerGate'), choices=qsa('[data-customer]',gate); const close=target=>{gate.classList.add('closing');gate.classList.remove('open');gate.setAttribute('aria-hidden','true');document.body.style.overflow='';setTimeout(()=>{gate.classList.remove('closing');if(target)qs(target)?.scrollIntoView({behavior:'smooth',block:'start'});},420);}; setTimeout(()=>{gate.classList.add('open');gate.setAttribute('aria-hidden','false');document.body.style.overflow='hidden';choices[0]?.focus();},350); choices.forEach(button=>button.addEventListener('click',()=>close(button.dataset.customer==='wholesale'?'#wholesale':null))); }
