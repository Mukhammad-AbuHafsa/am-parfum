import { PRODUCTS } from '../data/products.js';
import { qs } from '../core/dom.js';
const escapeHtml = value => String(value).replace(/[&<>'"]/g, character => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[character]));
export function initWholesale() { const rows=qs('#wholesalePriceRows'), search=qs('#wholesalePriceSearch'), empty=qs('#wholesaleListEmpty'); const render=query=>{const normalized=query.trim().toLocaleLowerCase('ru');const items=PRODUCTS.filter(item=>item.name.toLocaleLowerCase('ru').includes(normalized));rows.innerHTML=items.map((item,index)=>`<div class="wholesale-table__row" role="row"><span class="wholesale-table__name" role="cell"><i class="wholesale-table__number">${String(index+1).padStart(3,'0')}</i>${escapeHtml(item.name)}</span><span class="wholesale-table__price" role="cell">${escapeHtml(item.price)}</span></div>`).join('');empty.hidden=items.length!==0;rows.hidden=items.length===0;}; render(''); search.addEventListener('input',()=>render(search.value)); }
